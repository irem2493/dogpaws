<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dogpaws.backend.repository.dao.rim.ProductDao">
    <select id="getBestProducts" resultType="com.dogpaws.backend.dto.rim.ProductListDto">
        SELECT
        p.product_id,
        p.name,
        p.price,
        p.image_url,
        p.status,
        p.stock_quantity,
        p.main_category,
        p.manufacturer,
        COALESCE(COUNT(DISTINCT oi.ql_id), 0) as order_count
        FROM
        tbl_products p
        LEFT JOIN
        tbl_order_items oi ON p.product_id = oi.product_id
        LEFT JOIN
        tbl_orders o ON oi.ql_id = o.ql_id
        AND o.order_status NOT IN ('READY', 'CANCELLED')
        AND o.order_date >= DATE_SUB(NOW(), INTERVAL 30 DAY)
        WHERE
        p.status = 'O'
        AND p.main_category = #{category}
        GROUP BY
        p.product_id,
        p.name,
        p.price,
        p.image_url,
        p.status,
        p.stock_quantity,
        p.main_category,
        p.manufacturer
        ORDER BY
        order_count DESC,
        p.created_at DESC
        LIMIT #{size}
    </select>
    <select id="searchProducts" resultType="com.dogpaws.backend.dto.rim.ProductListDto">
        SELECT
        p.product_id,
        p.name,
        p.price,
        p.image_url,
        p.status,
        p.stock_quantity,
        p.main_category,
        p.created_at,
        p.manufacturer,
        (SELECT COUNT(*) FROM tbl_product_options po
        WHERE po.product_id = p.product_id) as option_count
        FROM tbl_products p
        WHERE 1=1
        <if test="status != null and status != ''">
            AND p.status = #{status}
        </if>
        <if test="mainCategory != null and mainCategory != ''">
            AND p.main_category = #{mainCategory}
        </if>
        <if test="subCategory != null and subCategory != ''">
            AND p.sub_category = #{subCategory}
        </if>
        <if test="searchKeyword != null and searchKeyword != ''">
            AND (
            p.name LIKE CONCAT('%', #{searchKeyword}, '%')
            OR p.description LIKE CONCAT('%', #{searchKeyword}, '%')
            )
        </if>
        <choose>
<<<<<<< HEAD
            <when test="sortBy == 'stock_asc'">
                ORDER BY p.stock_quantity ASC
            </when>
            <when test="sortBy == 'stock_desc'">
                ORDER BY p.stock_quantity DESC
=======
            <when test="sortBy != null and sortBy != ''">
                ORDER BY p.${sortBy} ${sortDirection}
>>>>>>> origin/REQ-68-관리자
            </when>
            <otherwise>
                ORDER BY p.created_at DESC
            </otherwise>
        </choose>
<<<<<<< HEAD
        <if test="pageSize != null and offset != null">
            LIMIT #{pageSize} OFFSET #{offset}
        </if>
=======
        LIMIT #{pageSize} OFFSET #{offset}
>>>>>>> origin/REQ-68-관리자
    </select>

    <select id="getTotalCount" resultType="int">
        SELECT COUNT(*)
        FROM tbl_products p
        WHERE 1=1
        <if test="status != null and status != ''">
            AND p.status = #{status}
        </if>
        <if test="mainCategory != null and mainCategory != ''">
            AND p.main_category = #{mainCategory}
        </if>
        <if test="subCategory != null and subCategory != ''">
            AND p.sub_category = #{subCategory}
        </if>
        <if test="searchKeyword != null and searchKeyword != ''">
            AND (
            p.name LIKE CONCAT('%', #{searchKeyword}, '%')
            OR p.description LIKE CONCAT('%', #{searchKeyword}, '%')
            )
        </if>
    </select>

    <select id="getProductOptions" resultType="com.dogpaws.backend.dto.rim.ProductOptionDto">
        SELECT
        option_id,
        option_name,
        option_price,
        option_stock,
        is_base_option as baseOption
        FROM tbl_product_options
        WHERE product_id = #{productId}
        ORDER BY is_base_option DESC, option_name ASC
    </select>
<<<<<<< HEAD

    <!-- 주문 진행중인 상품인지 확인 -->
    <select id="hasActiveOrders" resultType="boolean">
        SELECT EXISTS (
        SELECT 1
        FROM tbl_order_items oi
        JOIN tbl_orders o ON oi.ql_id = o.ql_id
        WHERE oi.product_id = #{productId}
        AND o.order_status NOT IN ('COMPLETED', 'CANCELLED')
        )
    </select>

    <!-- 상품 조회 (비관적 락) -->
    <select id="findByIdWithLock" resultType="com.dogpaws.backend.entity.rim.Product">
        SELECT
        product_id,
        name,
        price,
        stock_quantity,
        description,
        status,
        main_category,
        sub_category,
        material,
        origin,
        expiration_date,
        weight,
        image_url,
        image_detail_url,
        created_at,
        updated_at,
        manufacturer
        FROM tbl_products
        WHERE product_id = #{productId}
        FOR UPDATE
    </select>

    <!-- 옵션 조회 (비관적 락) -->
    <select id="findOptionByIdWithLock" resultType="com.dogpaws.backend.entity.rim.ProductOption">
        SELECT
        option_id,
        product_id,
        option_name,
        option_price,
        option_stock,
        is_base_option,
        option_size,
        option_color,
        option_weight,
        option_material,
        option_expiration_date,
        option_storage_info,
        option_manufacturer,
        option_origin
        FROM tbl_product_options
        WHERE option_id = #{optionId}
        FOR UPDATE
    </select>

    <!-- 상품의 전체 옵션 재고 합계 조회 -->
    <select id="getTotalStockByProductId" resultType="int">
        SELECT COALESCE(SUM(option_stock), 0)
        FROM tbl_product_options
        WHERE product_id = #{productId}
    </select>
    <!-- 상품 ID로 옵션 목록 조회 -->
    <select id="findByProductProductId" resultType="com.dogpaws.backend.entity.rim.ProductOption">
        SELECT
        option_id,
        product_id,
        option_name,
        option_price,
        option_stock,
        is_base_option,
        option_size,
        option_color,
        option_weight,
        option_material,
        option_expiration_date,
        option_storage_info,
        option_manufacturer,
        option_origin
        FROM
        tbl_product_options
        WHERE
        product_id = #{productId}
    </select>

=======
>>>>>>> origin/REQ-68-관리자
</mapper>
