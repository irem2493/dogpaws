<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dogpaws.backend.repository.dao.cys.ChatDao">
    <select id="getChatProfile" resultType="com.dogpaws.backend.dto.ajy.DogDto">
        SELECT *
        FROM tbl_dogs
        WHERE dog_id = #{id}
    </select>
</mapper>
